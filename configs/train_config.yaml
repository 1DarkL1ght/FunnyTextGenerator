data_path: data/concatenated_anekdot_dataset_v2_1_2M.csv
train_batch_size: 16
val_batch_size: 32
train_size: 0.99
epochs: 5
lr1: 2e-4
lr2: 2e-5
weight_decay: 0.05
fp16: True
max_len: 150
model_dir: outputs/models
optim: AdamW
device: cuda
patience: 5
inference_size: 4
seed: 42
latent_dim: 512
tsne: 1024
grad_accumulation_steps: 8
warmup_steps: 1 # in epochs
beta_max: 1
beta_anneal_steps: 1 # in epochs
lambda_: 0.2
scheduler_type: linear
word_dropout: 0.1
workers: 5
profile: True
n_tokens: 8
encoder_name: ai-forever/ruBert-base
decoder_name: ai-forever/rugpt3large_based_on_gpt2
name: exp_n_tokens_8_lambda_0.2_global_beta_max_1_word_dropout_0.1_ln_fc2_log_var_bias_-2_new_mapping_new_pretrained
lora: True
lora_modules:
  - c_attn

# resume: outputs/models/TransformerAnekdotGenerator_last.pt

# @TODO: REFACTOR ALL!!!


# word dropout с заменой случайных wte на нулевые вектора
# attention dropout
# z = z + eps, eps ~ N(0, s**2) и плавно уменьшать s до 0
# несколько шагов энкодер + маппинг перед обучением декодера
# VAMPPrior
# VAE-GAN

# LN после act и до mu, log_var
# log_var_layer.weight ~= 1e-4, bias=0
# global free-bits
# не один большой слой разжатия z, а 2 или больше, чтобы не резко взрывалось количество выходов
# bert-base + gpt3-large
# заморозка энкодера кроме последних 2 слоев
# Другой декодер на +- 1B параметров

# Перенести датасет в parquet, все на ssd nvme
